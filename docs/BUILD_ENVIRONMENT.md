# Build Environment

Use macOS as a practical founder/control-plane machine for Git, documentation, UI, Rust services, schemas, local databases, agents, and normal tests.

Use Linux with supported accelerator hardware for production-oriented GPU workloads and benchmark workers.

No workstation defines the protocol. Execution environments are explicit, versioned artifacts.
